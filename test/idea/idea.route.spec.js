let chai = require('chai');
let chaiHttp = require('chai-http');
let should = chai.should();
chai.use(chaiHttp);

var Idea = require(process.cwd() + '/src/story/story.model');

let expect = require('chai').expect


describe('Name of the group', function() {
  
});
  